package com.bcp.transactions.retiro;

import android.content.Context;
import com.newpos.libpay.trans.TransInputPara;

import org.junit.Before;
import org.junit.Test;

import static org.junit.Assert.assertEquals;


public class RetiroTest {

    Context context;
    TransInputPara p ;
    Retiro retiro;

    @Before
    public void Init() {
       //  retiro = new Retiro(context,"123",p);
    }
//    Retiro retiroMock = mock(Retiro.class);

    @Test
    public void getISO8583() {
     //   retiro.getISO8583();
      //  assertEquals(null,retiro.getISO8583());
    }

    @Test
    public void start() {
//        retiroMock.start();
//        verify(retiroMock).start();
    }
}